# SyclGen Kernel Optimization Progress
# Project: llama.cpp
# Target Hardware: Intel Arc Pro B60 (Xe2, Battlemage)
# Created: 2026-02-14

## Summary
- Total SYCL kernels to analyze: 33
- Kernels analyzed so far: 33
- Analysis complete: Yes
- Kernels with optimization potential: 32
- Kernels with no optimization needed: 1
- Optimization tasks created: 22 (in kernel_optimization_task_spec.json)

## Kernels Analyzed
- [x] sk_001: softmax SYCL kernels — high potential (6 optimization opportunities)
- [x] sk_002: rope SYCL kernels — medium potential (5 optimization opportunities)
- [x] sk_003: norm SYCL kernels — high potential (9 optimization opportunities)
- [x] sk_004: element_wise SYCL kernels — medium potential (8 optimization opportunities)
- [x] sk_005: convert SYCL kernels — medium potential (7 optimization opportunities)
- [x] sk_006: binbcast SYCL kernels — medium potential (6 optimization opportunities)
- [x] sk_007: cpy SYCL kernels — high potential (8 optimization opportunities)
- [x] sk_008: mmq SYCL kernels — critical potential (9 optimization opportunities)
- [x] sk_009: mmvq SYCL kernels — high potential (8 optimization opportunities)
- [x] sk_010: dmmv SYCL kernels — high potential (9 optimization opportunities)
- [x] sk_011: getrows SYCL kernels — medium potential (6 optimization opportunities)
- [x] sk_012: wkv SYCL kernels — medium potential (5 optimization opportunities)
- [x] sk_013: gla SYCL kernel — low potential (4 optimization opportunities)
- [x] sk_014: fill SYCL kernel — none potential (2 minor optimization opportunities)
- [x] sk_015: ggml_sycl_op_diag — low potential (3 optimization opportunities)
- [x] sk_016: ggml_sycl_expm1 — low potential (3 optimization opportunities)
- [x] sk_017: ggml_sycl_xielu — low potential (3 optimization opportunities)
- [x] sk_018: ggml_sycl_op_opt_step_sgd — low potential (3 optimization opportunities)
- [x] sk_019: ggml_sycl_op_opt_step_adamw — low potential (3 optimization opportunities)
- [x] sk_020: ggml_sycl_silu_back — low potential (4 optimization opportunities)
- [x] sk_021: ggml_sycl_op_get_rows_back — low potential (4 optimization opportunities)
- [x] sk_022: im2col_3d SYCL kernel — low potential (5 optimization opportunities)
- [x] sk_023: rope_back SYCL kernel — low potential (2 optimization opportunities, shares code with sk_002)
- [x] sk_024: ggml_sycl_op_conv_2d — medium potential (7 optimization opportunities)
- [x] sk_025: ggml_sycl_op_conv_2d_dw — medium potential (7 optimization opportunities)
- [x] sk_026: ggml_sycl_op_conv_transpose_2d — low potential (5 optimization opportunities)
- [x] sk_027: ggml_sycl_cross_entropy_loss — high potential (6 optimization opportunities)
- [x] sk_028: ggml_sycl_cross_entropy_loss_back — medium potential (7 optimization opportunities)
- [x] sk_029: ggml_sycl_op_top_k — high potential (6 optimization opportunities)
- [x] sk_030: ggml_sycl_op_cumsum — low potential (5 optimization opportunities)
- [x] sk_031: ggml_sycl_op_solve_tri — low potential (5 optimization opportunities)
- [x] sk_032: ggml_sycl_op_ssm_scan — medium potential (6 optimization opportunities)
- [x] sk_033: ggml_sycl_op_topk_moe — low potential (5 optimization opportunities)

## Recent Activity
[2026-02-27] opt_task_014 (vec4 vectorization): COMPLETED. Applied vec4 vectorization to soft_max_back_f32:
  - Added GGML_SYCL_VEC4 macro (default enabled)
  - Added vec4 loads for grad/dstf arrays in first loop (dot product computation)
  - Added vec4 loads/stores for dst array in second loop
  - Handles tail elements (non-multiple-of-4) with scalar fallback
  - Enabled via #if GGML_SYCL_VEC4 guard
  - Correctness: PASS (SOFTMAX, CROSS_ENTROPY_LOSS, CROSS_ENTROPY_LOSS_BACK unit tests pass)
  - Note: Other kernels (norm, element_wise, cross_entropy) have complex access patterns where threads process non-consecutive elements - unsafe to apply simple vec4 without major restructuring

[2026-02-27] opt_task_013 (native math functions): COMPLETED. Replaced standard sycl math functions with native:: variants:
  - rope.cpp: sycl::log->native::log, sycl::cos/sin->native::cos/sin, sycl::pow->exp2(log2())
  - element_wise.cpp: sycl::exp->native::exp, sycl::log->native::log, sycl::sqrt->native::sqrt, sycl::sin/cos->native::sin/cos
  - cross-entropy-loss.cpp: sycl::exp->native::exp, sycl::log->native::log
  - ssm_scan.cpp: sycl::exp->native::exp, sycl::log1p(sycl::exp(...))->native::versions
  - topk-moe.cpp: sycl::exp->native::exp
  - Note: sycl::native::tanh and sycl::native::expm1 not available in SYCL - kept as standard functions
  - Correctness: PASS (ROPE 248/248, GELU 8/8, SILU 8/8, TANH 8/8, SIGMOID 8/8, EXP 8/8, LOG 4/4, COS 4/4, SQRT 4/4)
  - Expected: 5-30% speedup depending on transcendental density

[2026-02-27] opt_task_012 (conv2d optimizations): COMPLETED. Applied 5 optimizations to conv2d.cpp:
  - Added kernel size specialization (1x1, 3x3, 5x5, 7x7 templates) — 20-40% expected improvement
  - Added [[sycl::reqd_sub_group_size(16)]] for native SIMD16 on Xe2 — 5-15% expected improvement
  - Changed int64_t to int32_t for all index arithmetic — 10-20% from faster integer ops
  - Replaced acc += x*y with sycl::fma(x, y, acc) — 1-5% expected improvement
  - Hoisted calculate_kernel_bounds outside c_in loop — 2-5% expected improvement
  - Correctness: PASS (480+ unit tests on b60)
  - Note: SLM tiling (opt_024_001) deferred — requires cooperative loading, complex restructuring
  - Benchmark: 38-194 GFLOPS depending on configuration (no baseline available)

[2026-02-27] opt_task_011 (convert reqd_sub_group_size): COMPLETED. Added [[sycl::reqd_sub_group_size(WARP_SIZE)]] to all ~25 kernel launch sites in convert.cpp:
  - Added attribute to dequantize_block_sycl (generic path)
  - Added to all Q2_K, Q3_K, Q4_0, Q4_1, Q5_K, Q6_K kernels (both 32 and 64 thread variants)
  - Added to all IQ types (iq1_s, iq1_m, iq2_xxs, iq2_xs, iq2_s, iq3_xxs, iq3_s, iq4_xs, iq4_nl, mxfp4)
  - Added to q4_K_reorder, q6_K_reorder kernels
  - Added to convert_unary_nc_sycl
  - Previously only q4_0_reorder had this attribute
  - Expected: 5-15% occupancy improvement from guaranteed SIMD16 on Xe2
  - Correctness: PASS (unit tests on b60)

[2026-02-27] opt_task_010 (cross-entropy work-group size): COMPLETED. Increased work-group size from 16 to 256 threads:
  - Changed work-group size from WARP_SIZE (16) to CE_WG_SIZE (256) for forward kernel cross_entropy_loss_f32
  - Changed work-group size from WARP_SIZE (16) to CE_WG_SIZE (256) for backward kernel cross_entropy_loss_back_f32
  - Replaced warp_reduce_max/sum with sycl::reduce_over_group for multi-sub-group reduction
  - Updated stride loops to use wg_size (item_ct1.get_local_range(2)) instead of WARP_SIZE constant
  - Fixed sum kernel (k_sum_f32) to use 256 threads instead of 16 with sycl::reduce_over_group
  - Precomputed grad/nrows as scalar in backward kernel instead of computing per-work-item
  - Correctness: PASS (4/4 unit tests on b60)
  - Note: Cross-entropy loss is training-only (zero wall-clock impact during inference)

[2026-02-27] opt_task_007 (mmvq work-group size): COMPLETED. Increased non-reorder work-group size from 32 to 128 threads:
  - Added GGML_SYCL_MMVQ_WG_SCALE constant (default=4) for work-group scaling
  - Changed work-group size from GGML_SYCL_MMV_Y * WARP_SIZE = 32 to GGML_SYCL_MMV_Y * WG_SCALE * WARP_SIZE = 128
  - Added WG_SCALE template parameter to mul_mat_vec_q kernel
  - Updated blocks_per_warp calculation to use WG_SCALE
  - Uses sycl::reduce_over_group for multi-sub-group reduction when WG_SCALE > 1
  - Updated work-group sizes for main kernels (Q4_0, Q4_1, Q5_0, Q5_1, Q8_0, Q2_K-Q6_K, MXFP4)
  - Expected: 2-3x throughput improvement from better memory latency hiding
  - Correctness: PASS (unit tests on b60)

[2026-02-27] opt_task_009 (wkv SLM over-allocation): COMPLETED. Fixed critical SLM bug and added sub_group_size:
  - Fixed SLM over-allocation in both wkv6 and wkv7 kernels
  - Changed shared_mem_size from byte count (C/H * 4 * sizeof(float)) to element count (C/H * 4)
  - wkv6: 4x SLM reduction (256 -> 1024 bytes for head_size=64)
  - wkv7: 4x SLM reduction (320 -> 1280 bytes for head_size=64)
  - Added [[sycl::reqd_sub_group_size(WARP_SIZE)]] to all 4 kernel launches
  - Expected: 25-50% occupancy improvement from freeing 3/4 of wasted SLM per work-group
  - Correctness: PASS (4/4 tests passed on b60)
  - Note: WKV is used by RWKV attention in state-space models, not transformer inference

[2026-02-27] opt_task_008 (norm kernels): COMPLETED. Fixed group_norm global barriers and increased work-group size:
  - Fixed 2 global barriers in group_norm_f32: changed barrier() to barrier(local_space)
  - Removed 8 lines of DPCT migration comments
  - Increased small-path work-group from 16 to 256 threads in norm_f32, rms_norm_f32, l2_norm_f32
  - Added SLM allocation for small path (64-128 bytes per work-group)
  - Expected 2-4x throughput improvement from better occupancy (16x more parallelism per row)
  - Correctness: PASS (43/43 unit tests on b60)
  - Note: Norm operations run after every attention/FFN block - high impact on inference path

[2026-02-27] opt_task_005 (cpy work-group size): COMPLETED. Optimized cpy kernels:
  - Increased SYCL_CPY_BLOCK_SIZE from 32 to 256 (scalar copy work-groups)
  - Increased quantized copy work-groups from 1 to 256 (f32->q, q->f32, q->q kernels)
  - Added [[sycl::reqd_sub_group_size(WARP_SIZE)]] to all 20+ kernel launches
  - f32->q4_0: 6.72x speedup (804.77 us -> 119.69 us)
  - q4_0->f32: 8.71x speedup (3946.24 us -> 453.45 us)
  - Correctness: PASS (131/131 unit tests on b60)
  - Note: Quantized copy with work-group=1 was catastrophically bad - 15/16 SIMD lanes wasted on Xe2

[2026-02-27] opt_task_003 (top_k SLM overflow fix): COMPLETED. Fixed critical correctness bugs in top-k kernel:
  - Added k=1 fast path using argmax_f32_i32_sycl directly — ~1000x faster for greedy decoding
  - Added SLM overflow fallback in argsort_f32_i32_sycl — handles vocab sizes > 16384 (was crashing)
  - Added [[sycl::reqd_sub_group_size(16)]] to argsort (ASC/DESC) and argmax kernels
  - Made argmax_f32_i32_sycl non-static for use by top-k.cpp
  - Correctness: PASS (unit tests passed on b60)
  - Note: This was a CRITICAL correctness fix — kernel crashed for typical LLM vocab sizes (32K-128K)

[2026-02-27] opt_task_002 (dmmv QK_WARP_SIZE fix): COMPLETED. Fixed K-quant kernels in dmmv.cpp:
  - Changed work-group size from QK_WARP_SIZE (32) to WARP_SIZE (16) for all 5 K-quant kernels (Q2_K through Q6_K)
  - Changed reqd_sub_group_size from QK_WARP_SIZE to WARP_SIZE for native SIMD16 on Intel Xe2
  - Changed reduction loop mask from QK_WARP_SIZE/2 to WARP_SIZE/2 for correct 16-thread reduction
  - Fixed off-by-one boundary bug: changed 'row > nrows' to 'row >= nrows' in all 4 affected kernels
  - Expected: 15-40% occupancy improvement from using native SIMD16 instead of forced SIMD32
  - Correctness: PASS (unit tests passed on b60)
  - Note: Full multi-sub-group reduction (opt_010_001) with larger work-groups (128-256) pending

[2026-02-27] opt_task_004 (softmax reqd_sub_group_size): COMPLETED. Added [[sycl::reqd_sub_group_size(WARP_SIZE)]] to:
  - Fallback forward kernel (line 293-294 in softmax.cpp)
  - Backward kernel (line 317-318 in softmax.cpp)
  - This fixes a correctness risk where the compiler may choose SIMD32 instead of SIMD16, causing incorrect results for warp-level operations.
  - Correctness: PASS (unit tests passed on b60)
  - Risk: low - this is a compiler hint that ensures proper sub-group size for warp-level primitives

[2026-02-27] opt_task_001 status changed to pending: XMX joint_matrix implementation is too complex (~1000+ lines) for single session. Partial work done: XE2 tile sizes for Q4_0/Q8_0.

[2026-02-27] opt_task_001 (XMX joint_matrix): Started. Due to complexity (~1000+ lines), focused on related tile size tuning (part of opt_task_006). Added XE2 tile size defines for Q4_0 and Q8_0:
  - MMQ_X_Q4_0_XE2 = 64, MMQ_Y_Q4_0_XE2 = 64, NWARPS_Q4_0_XE2 = 8
  - MMQ_X_Q8_0_XE2 = 64, MMQ_Y_Q8_0_XE2 = 64, NWARPS_Q8_0_XE2 = 8
  - Updated mul_mat_q4_0 and mul_mat_q8_0 kernels to use XE2 tile sizes when SYCL_USE_XMX is defined.
  - Addresses part of opt_008_003 (tune tile sizes for Intel Xe2). Old AMPERE tile sizes were for NVIDIA GPUs.
  - Expected: 20-50% from properly tuned tile sizes matching Xe2 (64KB SLM, SIMD16).
  - Note: Full XMX joint_matrix (opt_task_001) still pending - requires ~1000+ lines new code.

[2026-02-27] Analyzed kernel ggml_sycl_op_topk_moe (sk_033). Found 5 optimization opportunities (low potential).
  - HIGH: Missing #pragma unroll on ALL inner loops — CUDA has ~12 explicit unroll directives; SYCL has zero. 10-25% speedup, especially for large n_experts (256-576) where experts_per_thread is large.
  - MEDIUM: Standard sycl::exp instead of sycl::native::exp in softmax and sigmoid — CUDA uses fast-math expf(). 5-15% from faster transcendentals.
  - LOW: Replace dpct::permute_sub_group_by_xor with sycl::sub_group::shuffle_xor — DPCT migration artifact, functionally identical, 0-2%.
  - LOW: Consider rows_per_block=8 for better XVE occupancy — current 64 threads = 50% occupancy; 128 threads = 100%.
  - LOW: Clean dpct::queue_ptr artifact, consider replacing macro with template helper.
  - Note: One of the best-ported SYCL kernels (75-85% efficiency). Already has reqd_sub_group_size, clean 2D nd_range, proper template specialization, coalesced memory access, efficient register-only computation. Used for MoE expert routing — lightweight per-invocation.

[2026-02-27] ALL 33 SYCL KERNELS ANALYZED. Analysis phase complete. Finalization pending.

[2026-02-27] Analyzed kernel ggml_sycl_op_ssm_scan (sk_032). Found 6 optimization opportunities (medium potential).
  - HIGH: Missing #pragma unroll on all inner loops — CUDA explicitly unrolls every inner loop. 15-30% overall kernel speedup.
  - HIGH: Missing L_template specialization for Mamba-1 — CUDA specializes for L=1..8. 20-50% speedup for L=1 autoregressive decoding.
  - MEDIUM: Standard sycl::exp instead of native — 10-20% for Mamba-1 (exp in hot inner loop).
  - MEDIUM: Missing reqd_sub_group_size on Mamba-1 kernel launch — 5-15% occupancy improvement.
  - MEDIUM: Non-coalesced A/s0 loads in Mamba-1 — missing CUB BlockLoad WARP_TRANSPOSE equivalent.
  - LOW: DPCT artifacts — dpct::queue_ptr, nd_range<3> vs <2> inconsistency, no launch_bounds equivalent.

[2026-02-27] Analyzed kernel ggml_sycl_op_solve_tri (sk_031). Found 5 optimization opportunities (low potential).
  - MEDIUM: Missing #pragma unroll on hot inner loops — CUDA version explicitly unrolls all 6 loops in forward substitution. 5-15% for template-specialized n=64 paths.
  - LOW: Replace dpct-migrated warp_reduce_sum with sycl::reduce_over_group — idiomatic SYCL, 0-2% performance difference.
  - LOW: Missing [[sycl::reqd_sub_group_size(16)]] on batch pointer setup kernel.
  - LOW: DPCT artifacts — item_ct1 naming, get_local_id(2)/get_local_id(1) convention.
  - LOW: SLM always allocated at MAX_N_FAST*MAX_N_FAST (16KB) regardless of actual n — reduces concurrent work-group occupancy for small n.
  - Note: Well-ported kernel with generalized NUM_SECTIONS approach for variable sub-group size. oneMKL trsm_batch path for large matrices is already optimal. Used exclusively by Mamba/SSM models, not on transformer inference critical path.

[2026-02-27] Analyzed kernel ggml_sycl_op_cumsum (sk_030). Found 5 optimization opportunities (low potential).
  - MEDIUM: Missing CUB DeviceScan/BlockScan equivalent — SYCL only implements CUDA's fallback two-level scan path. For long rows (ne00 > 4096), oneDPL inclusive_scan could provide 2-4x speedup.
  - LOW: Scalar loads/stores — vec4 vectorization would improve memory throughput 10-20% (each thread already processes 4 contiguous elements).
  - LOW: Missing #pragma unroll on 3-4 iteration inner loops that CUDA version explicitly unrolls.
  - LOW: DPCT artifacts — 3D nd_range with block_dims=(1,1,N), item_ct1 naming, get_local_id(2) indexing.
  - LOW: No contiguous fast-path — always uses element strides even for contiguous tensors.
  - Note: Kernel already uses idiomatic sycl::inclusive_scan_over_group for sub-group prefix scan. Has correct reqd_sub_group_size(16). SLM usage is minimal (~1KB). Near-optimal for short rows (ne00 <= 1024).

[2026-02-27] Analyzed kernel ggml_sycl_op_top_k (sk_029). Found 6 optimization opportunities (high potential).
  - CRITICAL: Uses O(n log² n) full bitonic argsort instead of O(n) radix-select for top-k — CUDA uses CUB DeviceTopK::MaxPairs. For greedy decoding (k=1), existing argmax would be ~1000x faster.
  - CRITICAL: SLM overflow bug — allocates ncols_pad * sizeof(int) in SLM, which is 128KB-512KB for typical LLM vocabularies (32K-128K tokens), far exceeding the 64KB Xe-core limit. Crashes at runtime via GGML_ASSERT.
  - HIGH: Allocates ncols * nrows temporary int32 buffer for full sort results, then copies only k per row — massive memory waste.
  - HIGH: No fast-path for k=1 (greedy decoding) — most common inference case could use simple argmax.
  - MEDIUM: Missing [[sycl::reqd_sub_group_size(16)]] on both argsort and copy kernels.
  - LOW: DPCT artifacts — 3D nd_range with trivial dims, dpct::queue_ptr, manual get_multi_ptr extraction.

[2026-02-27] Analyzed kernel ggml_sycl_cross_entropy_loss_back (sk_028). Found 7 optimization opportunities (medium potential).
  - HIGH: Work-group size is only WARP_SIZE (16 threads) — 4-8x throughput gain possible by increasing to 256+ with multi-sub-group reduction. Identical to forward kernel (sk_027).
  - HIGH: Redundant logits read from global memory in non-SLM path — online softmax could eliminate extra write+read (20-40% memory traffic reduction).
  - MEDIUM: Scalar-only loads across 2 passes — vec4 vectorization would reduce memory transactions by 4x.
  - MEDIUM: Standard sycl::exp instead of sycl::native::exp — 10-20% from faster transcendentals.
  - MEDIUM: *grad/nrows computed by every work-item instead of precomputed as kernel argument.
  - LOW: No #pragma unroll on hot stride loops.
  - LOW: DPCT artifacts — nd_range<3>, dpct::queue_ptr, item_ct1, dpct shuffle wrappers.

[2026-02-27] Analyzed kernel ggml_sycl_cross_entropy_loss (sk_027). Found 6 optimization opportunities (high potential).
  - HIGH: Work-group size is only WARP_SIZE (16 threads) — 4-8x throughput gain possible by increasing to 256+ with multi-sub-group reduction.
  - HIGH: Sum kernel k_sum_f32 uses only 16 threads in 1 work-group — CUDA uses CUB DeviceReduce::Sum. Replace with multi-level parallel reduction.
  - MEDIUM: Scalar-only loads across 3 passes — vec4 vectorization would reduce memory transactions by 4x.
  - MEDIUM: Standard sycl::exp/log instead of native variants — 10-20% from faster transcendentals.
  - MEDIUM: No #pragma unroll on any of the 4 hot stride loops.
  - LOW: DPCT artifacts — nd_range<3>, dpct::queue_ptr, item_ct1, dpct shuffle wrappers.

[2026-02-27] Analyzed kernel ggml_sycl_op_conv_transpose_2d (sk_026). Found 5 optimization opportunities (low potential).
  - MEDIUM: Eliminate stride-alignment divergence — restructure kh/kw loops to only iterate valid pairs (15-30% for stride > 1). Both CUDA and SYCL have this issue.
  - LOW: Add [[sycl::reqd_sub_group_size(16)]] — 5-10% occupancy improvement on Xe2.
  - LOW: Replace manual global_id computation with item.get_global_id(0).
  - LOW: Clean queue_ptr DPCT artifact.
  - LOW: Add #pragma unroll on small kh/kw loops.

[2026-02-27] Analyzed kernel ggml_sycl_op_conv_2d_dw (sk_025). Found 7 optimization opportunities (medium potential).
  - HIGH: Replace int64_t with int32_t for all index arithmetic — CUDA uses int32 throughout; int64 division is 10-20x slower on Intel GPUs.
  - MEDIUM: Add [[sycl::reqd_sub_group_size(16)]] — 5-15% occupancy improvement on Xe2.
  - MEDIUM: Add kernel size specialization templates (3×3, 5×5, 7×7) — enables compile-time loop unrolling.
  - LOW: Use item.get_global_id(0) instead of manual computation.
  - LOW: Add F16 compute path (CUDA has template<T>).
  - LOW: SLM tiling potential for larger kernel sizes.
  - LOW: Params struct size reduction from 120→60 bytes.

[2026-02-27] Analyzed kernel ggml_sycl_op_conv_2d (sk_024). Found 7 optimization opportunities (medium potential).
  - HIGH: Add SLM tiling for input data reuse — 30-60% improvement for 3×3+ convolutions from eliminating redundant global memory reads. Conv2d has highest data reuse ratio of common NN ops.
  - HIGH: Add kernel size specialization (1×1, 3×3, 5×5, 7×7) — 20-40% from compile-time loop unrolling of inner ky/kx loops.
  - MEDIUM: Replace int64_t with int32_t for all index arithmetic — 10-20% from faster GPU integer division/modulo.
  - MEDIUM: Add [[sycl::reqd_sub_group_size(16)]] — 5-15% from optimal SIMD width on Xe2.
  - LOW: Hoist calculate_kernel_bounds outside c_in loop — 2-5% from eliminating redundant invariant computation.
  - LOW: Use explicit sycl::fma — 1-5% from guaranteed fused multiply-add.
  - LOW: Missing CWHN layout support — feature parity gap with CUDA backend.

[2026-02-27] Analyzed kernel ggml_sycl_rope_back (sk_023). Found 2 optimization opportunities (low potential).
  - MEDIUM: Shares 100% code with rope forward (sk_002) via template<forward>. All 5 sk_002 optimizations (native sin/cos, pow→exp2/log2, reqd_sub_group_size, 2D nd_range, DPCT cleanup) apply automatically. Training-only kernel — zero wall-clock impact during inference.
  - LOW: Missing ROPE+VIEW+SET_ROWS fusion and mixed F32→F16 type support that CUDA has (currently forward-only in CUDA, but feature parity gap).

[2026-02-27] Analyzed kernel ggml_sycl_op_im2col_3d (sk_022). Found 5 optimization opportunities (low potential).
  - MEDIUM: Missing downsample_sycl_global_range — im2col_2d uses this for int overflow protection but im2col_3d does not.
  - LOW: Scalar-only memory access — vec4 stores would improve write throughput 20-40%.
  - LOW: Missing [[sycl::reqd_sub_group_size(16)]] — 5-10% occupancy improvement on Xe2.
  - LOW: DPCT artifacts — dpct::queue_ptr, manual thread indexing instead of get_global_id(0).
  - LOW: No F16 output path — CUDA supports both F16 and F32 dst, SYCL only F32.

[2026-02-27] Analyzed kernel ggml_sycl_op_get_rows_back (sk_021). Found 4 optimization opportunities (low potential).
  - MEDIUM: Replace brute-force O(nrows_grad*nrows_dst) serial scan with atomic scatter-add O(nrows_grad) — 10-25x speedup for typical training workloads where nrows_dst >> nrows_grad.
  - LOW: Add vectorized loads/stores (vec4) — 20-40% throughput improvement for memory-bound portion.
  - LOW: Add [[sycl::reqd_sub_group_size(16)]] — 5-10% occupancy improvement on Xe2.
  - LOW: Clean DPCT artifacts — 3D→2D nd_range, item_ct1 naming, manual thread indexing.

[2026-02-27] Analyzed kernel ggml_sycl_silu_back (sk_020). Found 4 optimization opportunities (low potential).
  - LOW: Uses standard sycl::exp instead of sycl::native::exp — inconsistent with forward silu which uses native. 5-15% improvement possible.
  - LOW: Scalar-only memory access — vec4 vectorized loads/stores would improve throughput 20-40% but near-zero wall-clock impact (training-only kernel).
  - LOW: Missing [[sycl::reqd_sub_group_size(16)]] — 5-10% occupancy improvement on Xe2.
  - LOW: DPCT artifacts — dpct::queue_ptr, __dpct_inline__, manual thread indexing instead of get_global_id(0).

[2026-02-27] Analyzed kernel ggml_sycl_op_opt_step_adamw (sk_019). Found 3 optimization opportunities (low potential).
  - LOW: Add vectorized loads/stores (vec4) — 20-40% kernel throughput but near-zero wall-clock impact since AdamW runs once per training batch.
  - LOW: Precompute pars[] (7 floats) as scalar kernel arguments instead of global memory pointer — 1-3% improvement.
  - LOW: Add [[sycl::reqd_sub_group_size(16)]], replace manual thread indexing with get_global_id(0), clean DPCT artifacts.

[2026-02-27] Analyzed kernel ggml_sycl_op_opt_step_sgd (sk_018). Found 3 optimization opportunities (low potential).
  - LOW: Add vectorized loads/stores (vec4) — 20-40% kernel throughput improvement but near-zero wall-clock impact since SGD step is called once per training batch.
  - LOW: Precompute lr/wd as scalar kernel arguments instead of global memory pointer — 1-3% from eliminating broadcast reads (L1 cache already handles this well).
  - LOW: Add [[sycl::reqd_sub_group_size(16)]], replace manual thread indexing with get_global_id(0), clean DPCT artifacts.

[2026-02-27] Analyzed kernel ggml_sycl_xielu (sk_017). Found 3 optimization opportunities (low potential).
  - MEDIUM: Add vectorized loads/stores (vec4) — 20-40% memory throughput improvement for this memory-bound element-wise kernel.
  - LOW: Add [[sycl::reqd_sub_group_size(16)]] to both F16 and F32 launch paths — 5-10% occupancy improvement on Xe2.
  - LOW: Consider native::exp decomposition for expm1 — 2-5% improvement but with precision trade-off for small negative values.

[2026-02-27] Analyzed kernel ggml_sycl_expm1 (sk_016). Found 3 optimization opportunities (low potential).
  - MEDIUM: Add contiguous fast-path to ggml_sycl_op_unary — eliminates 6 int64 div/mod per element for contiguous tensors (10-25% improvement). Applies to all ~25 unary ops.
  - LOW: Add vectorized loads/stores (vec4) to contiguous path — 20-40% additional memory throughput improvement.
  - LOW: Add [[sycl::reqd_sub_group_size(16)]] to unary op framework — 5-10% occupancy improvement on Xe2.

[2026-02-27] Analyzed kernel ggml_sycl_op_diag (sk_015). Found 3 optimization opportunities (low potential).
  - LOW: Redundant 4D index decomposition — dst_idx equals global_idx for contiguous output; can eliminate 2 of 3 int64 divisions.
  - LOW: Missing [[sycl::reqd_sub_group_size(16)]] on both kernel launch paths.
  - LOW: DPCT artifacts — dpct::queue_ptr, manual thread indexing instead of get_global_id(0).

[2026-02-27] Analyzed kernel fill (ggml_sycl_op_fill) SYCL kernel (sk_014). Found 2 optimization opportunities (none/near-optimal potential).
  - LOW: DPCT artifact — dpct::queue_ptr parameter type instead of sycl::queue&.
  - LOW: Verbose F16 conversion — ggml_fp32_to_fp16 followed by reinterpret_cast instead of sycl::half(value).

[2026-02-14] Analyzed kernel gla (Gated Linear Attention) SYCL kernel (sk_013). Found 4 optimization opportunities (low potential).
  - MEDIUM: Missing reqd_sub_group_size(16) on kernel lambda — suboptimal SIMD width on Xe2.
  - MEDIUM: Missing #pragma unroll on hot inner j-loop — compile-time constant trip count of 16 or 32.
  - LOW: DPCT migration artifacts — dpct::queue_ptr, u_int non-standard type.
  - LOW: Scalar global memory loads for k/r/td/v — could use vec4 to match SLM read pattern.

[2026-02-14] Analyzed kernel wkv (RWKV WKV attention) SYCL kernels (sk_012). Found 5 optimization opportunities (medium potential).
  - HIGH: SLM over-allocation by 4x — local_accessor<float> receives C/H*N*sizeof(float) (byte count) but expects element count. Wastes 3/4 of allocated SLM.
  - MEDIUM: Scalar SLM loads for float4 construction — 4 individual reads instead of 1 aligned 128-bit vector load (CUDA uses (float4&) cast).
  - MEDIUM: Missing reqd_sub_group_size(16) on all 4 kernel launch paths.
  - LOW: 3D nd_range with dims 0,1 always 1 — DPCT artifact, should be 1D.
  - LOW: DPCT artifacts — dpct::queue_ptr, item_ct1 naming, verbose get_multi_ptr extraction.

[2026-02-14] Analyzed kernel getrows (gather rows) SYCL kernels (sk_011). Found 6 optimization opportunities (medium potential).
  - MEDIUM: Missing I32 and BF16 src0 types + non-F32 dst type support — CUDA has two-level type switch covering I32, BF16, F16, F32 src and F32, I32, F16, BF16 dst.
  - MEDIUM: No reqd_sub_group_size(16) on any kernel launch — suboptimal SIMD width on Xe2.
  - MEDIUM: Integer div/mod for batch indexing (i11/i12 = global_id / ne12, % ne12) — expensive; CUDA uses grid-stride loops with grid clamping instead.
  - LOW: No vectorized loads/stores for float gather path.
  - LOW: Unnecessary dpct::has_capability_or_fail(fp16) in get_rows_sycl_float.
  - LOW: k_get_rows_back_float uses nd_range<3> with dim 0 = 1 — could be nd_range<2>.

[2026-02-14] Analyzed kernel dmmv (dequantize matrix-vector multiply) SYCL kernels (sk_010). Found 9 optimization opportunities (high potential).
  - CRITICAL: Work-group sizes only 16-32 threads — CUDA uses 32-256 with dynamic selection. Only 1 hardware thread per XVE, 4-8x throughput loss.
  - HIGH: QK_WARP_SIZE=32 forces SIMD32 on Intel for K-quant kernels — halves occupancy vs native SIMD16.
  - HIGH: Off-by-one boundary bug — K-quant kernels Q2_K/Q3_K/Q4_K/Q6_K use 'row > nrows' instead of 'row >= nrows'.
  - HIGH: No multi-column batching — 1 output column per launch vs CUDA's 8.
  - HIGH: ~620 lines of duplicated K-quant code + 11 identical dispatch functions.
  - MEDIUM: Manual dpct shuffle reduction, no vectorized Y loads, no GLU/bias fusion.
  - LOW: DPCT artifacts, missing BF16, dead GGML_SYCL_F16 code for K-quants.

[2026-02-14] Analyzed kernel mmvq (quantized matrix-vector multiply) SYCL kernels (sk_009). Found 8 optimization opportunities (high potential).
  - HIGH: One kernel launch per output column — CUDA batches up to 8, causing 4x redundant weight reads
  - HIGH: Non-reorder work-group size only 16 (CUDA uses 128) — minimal memory latency hiding
  - HIGH: 31 duplicated functions (11 kernels + 20 dispatchers) — should be unified template
  - MEDIUM: No GLU/bias fusion — CUDA fuses gate matmul + bias + activation into single kernel
  - MEDIUM: Inconsistent reduction patterns — reorder uses reduce_over_group, non-reorder uses dpct shuffle
  - MEDIUM: GGML_SYCL_MMV_Y=1 vs CUDA rows_per_block=2 — half the row throughput
  - LOW: DPCT artifacts, reorder path assertion crash risk for unusual model dimensions

[2026-02-14] Analyzed kernel mmq (quantized matrix multiply) SYCL kernels (sk_008). Found 9 optimization opportunities (critical potential).
  - CRITICAL: No XMX/tensor core compute path — SYCL_USE_XMX only changes tile sizes, not computation. joint_matrix completely unused. 3-5x potential.
  - HIGH: Tile size mismatch bug — dispatch selects sizes by compute_capability but kernels hardcoded to AMPERE constants
  - HIGH: Tile sizes tuned for NVIDIA Ampere, not Intel Xe2 (different SLM, SIMD width, register file)
  - HIGH: No stream-K partitioning for better EU utilization on small batches (15-40% improvement)
  - MEDIUM: Y data loaded from global memory every K sub-iteration instead of pre-reformatted layout
  - MEDIUM: No reqd_sub_group_size(16), global barriers instead of local barriers
  - MEDIUM: 10 nearly identical dispatch functions should be one template
  - LOW: Missing 8 IQ quant types + MXFP4, pervasive DPCT artifacts

[2026-02-14] Analyzed kernel cpy (copy) SYCL kernels (sk_007). Found 8 optimization opportunities (high potential).
  - CRITICAL: Quantized copy kernels launch with work-group size 1 — 15/16 SIMD lanes wasted on Xe2
  - HIGH: Scalar copy work-group size is only 32 (SYCL_CPY_BLOCK_SIZE) — should be 256+
  - HIGH: No contiguous fast-path — always performs 12 integer div/mul for 4D index decomposition
  - MEDIUM: No vectorized loads/stores, no #pragma unroll, no reqd_sub_group_size(16)
  - LOW: DPCT artifacts, int32 indices (overflow risk), missing BF16 support

[2026-02-14] Analyzed kernel binbcast (binary broadcast) SYCL kernels (sk_006). Found 6 optimization opportunities (medium potential).
  - HIGH: Uses plain % and / for broadcast indices — CUDA uses fastdiv/fastmodulo (10x cheaper on GPU)
  - MEDIUM: Missing fused multi-source op support — CUDA fuses up to 8 adds into single kernel
  - MEDIUM: Fallback condition only checks z-dim limit, missing y-dim check — potential correctness bug
  - LOW: Missing repeat_back kernel, DPCT artifacts, missing reqd_sub_group_size

[2026-02-14] Analyzed kernel convert (dequantize) SYCL kernels (sk_005). Found 7 optimization opportunities (medium potential).
  - HIGH: Missing optimized Q8_0→FP16 kernel — CUDA has dequantize_block_q8_0_f16 with SLM+half2 for 2-3x speedup
  - MEDIUM: Missing reqd_sub_group_size on all kernels except q4_0_reorder — 5-15% occupancy loss
  - MEDIUM: Non-contiguous (NC) dequantize gap — SYCL only supports F32/BF16→FP16 NC, CUDA supports all basic quant types
  - LOW: DPCT artifacts (nd_range<3>, has_capability_or_fail, __dpct_inline__), missing BF16 first-class support

[2026-02-14] Analyzed kernel element_wise SYCL kernels (sk_004). Found 8 optimization opportunities (medium potential).
  - MEDIUM: All ops use scalar loads — vec4 vectorization would improve memory throughput 20-40%
  - MEDIUM: Inconsistent native math — silu/sigmoid use native::exp but gelu/exp/sin/cos/log use standard versions
  - LOW: 5 separate GLU kernels duplicate index math — should consolidate to template
  - LOW: swiglu_oai uses 3D nd_range (DPCT artifact), dpct_inline cleanup, generic stride overhead
  - LOW: Feature gaps — acc_f32 3D only, upscale missing bilinear/bicubic modes

[2026-02-14] Analyzed kernel norm SYCL kernels (sk_003). Found 9 optimization opportunities (high potential).
  - HIGH: group_norm uses global barrier instead of local_space — performance bug
  - HIGH: Three inconsistent reduction patterns — should use sycl::reduce_over_group
  - HIGH: rms_norm uses only 16 threads for ncols<1024 — 2-4x throughput loss vs CUDA
  - MEDIUM: No vectorized loads (scalar only) — memory throughput improvement possible
  - MEDIUM: Missing fused rms_norm_multiply kernel (CUDA has it, eliminates a full memory pass)
  - MEDIUM: Standard sycl::rsqrt instead of native
  - LOW: DPCT artifact cleanup, l2_norm out-of-date with CUDA, group_norm batch dimension bug

[2026-02-14] Analyzed kernel rope SYCL kernels (sk_002). Found 5 optimization opportunities (medium potential).
  - HIGH: Use sycl::native::cos/sin instead of standard versions — 15-30% speedup on Xe2
  - HIGH: Replace sycl::pow with native::exp2(log2()*...) — 10-20% speedup
  - MEDIUM: Add [[sycl::reqd_sub_group_size(16)]] to all 8 launch paths — 5-15% occupancy improvement
  - LOW: Simplify 3D nd_range to 2D (DPCT artifact cleanup)
  - LOW: Clean up manual thread indexing, remove DPCT artifacts

[2026-02-14] Analyzed kernel softmax SYCL kernels (sk_001). Found 6 optimization opportunities (high potential).
  - CRITICAL: Missing [[sycl::reqd_sub_group_size(WARP_SIZE)]] on 2 kernel launch paths — correctness risk
  - HIGH: Manual SLM block reduction with 5 barriers can be replaced with sycl::reduce_over_group
  - MEDIUM: No vectorized memory access (scalar float only) — 20-40% memory throughput improvement possible
  - MEDIUM: soft_max_back_f32 work-group size too small (only 16 on Intel)
